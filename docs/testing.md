# 开发与验收记录

更新日期：2026-09-24。验收对象：单侧自然眼线 Demo。

## v0.3 工程验证

最新实现、测试命令、截图与仍待执行的真人验收见 [v0.3 交接](v0.3-handoff.md)。以下0.2记录保留为历史，不代替新版验证。

## 0.2 引导优化验证（2026-09-24）

用户已确认验收首版，并同意推进轮廓、分步提示和放大画面的优化；本记录不把该确认推导成未提供的真实样本统计。

- `npm run test`：35 项通过。新增左右眼轮廓对称性、渐细眼尾与原方向轴一致性、轮廓随头部旋转/缩放、放大裁切边界测试。
- 新增 OpenCV 兼容测试：左右眼、三组角度/长度、仅眼尾与连接外段后的完整轮廓，均用合成图片验证方向仍接近原目标。这是算法兼容性检查，不是真人准确率。
- `npm run test:e2e`：13 项通过。新增示意图与实拍状态区分、模式切换、参数与轮廓联动、当前段提示切换、放大画面在暂停/丢失时清空。
- 原始图隔离测试扩展到深色妆效预览和放大画面，仍不把任何虚拟轮廓作为分析输入。
- `npm run build`：通过。已人工查看移动视口的示意轮廓、妆效预览、眼尾与连接步骤截图，并在浏览器切换显示模式。
- 新版手机操作与真实上妆验证仍需补充：起/收和箭头是否易懂、放大图和操作按钮是否易于同时查看、轮廓宽度是否适合练习。单纯切换显示模式不应被理解为新的纠偏算法。

本版截图在 `test-results/v2-shape-guide.png`、`v2-shape-preview.png`、`v2-live-wing.png`、`v2-live-connect.png`。两个 live 截图来自测试合成视频，不能作为真人效果展示。

## 首版开发执行记录（2026-09-23）

以下为首版开发时的记录。自动化测试证明代码与流程按预期工作；当时尚无真实样本指标、实体手机五轮或三位新手试用记录。

本次执行记录：

- `npm run test`：29 项通过（包含合成图算法和验收门槛计算）。
- Playwright 页面测试：11 项通过，已使用本机 Chrome 和 Playwright Chromium 验证；其中模型测试使用真实 MediaPipe，流程测试使用合成视频与定位替身。
- `npm run build`：TypeScript 检查与生产构建通过，生产包不包含相机测试替身。
- `npm run test:vision`：未提供真实样本时报告 `NOT RUN`（退出码 2）。另用临时非人脸图片验证了批处理读取、模型推理和报告生成：正确拒判且验收为 `false`；这只是执行通路测试，不计入真实样本数量。
- 开发服务器对 `samples/private/` 的访问返回 403；样本不放入 `public/`。
- 手机、三位新手和真实样本正确率：**未执行**。

| 层级 | 检查内容 | 证据边界 |
|---|---|---|
| Vitest | 左右眼/镜像/缩放/旋转、10° 边界、重复请求、过期结果、暂停与步骤推进 | 确定输入的逻辑正确性 |
| OpenCV 合成图 | 接近/偏高/偏低、未画、模糊、大区域遮挡、目标和姿态不一致、重复运行 | 真正运行 OpenCV，图片是合成的，不能作为真实样本准确率 |
| Playwright | 完整两段流程、取消/重试、相机拒绝、模型失败、人脸丢失、OpenCV 失败、侧别/参数修改 | 浏览器移动视口；视觉定位替身仅用于隔离页面逻辑 |
| MediaPipe 冒烟测试 | 从本站加载真实 WASM 和模型，对合成非人脸画面运行推理，正确无检测 | 模型可加载和执行；不证明真人眼部跟踪质量 |
| 原始图隔离 | 确认 Canvas 确实有路径，开关路径、镜像和调整窗口，再比较原始视频像素 | Chromium 视频颜色转换实测会有最多 1 灰阶的舍入差，验收容差为单通道 ≤1、全图均差 <0.1；虚拟绿线会超过此界限 |
| 布局 | 移动/桌面视口截图与横向溢出检查 | 不代表实体手机测试 |

## 执行自动化

唯一官方 E2E 验收命令是 `npm run test:e2e`。它使用
`playwright.config.ts` 中统一配置的 Worker 数量；不要通过命令行覆盖并发数。

```sh
npm run test
npm run test:e2e
npm run build
```

浏览器首次安装：`npx playwright install chromium`。可选择本机 Google Chrome：`PW_CHANNEL=chrome npm run test:e2e`。

测试产生的截图/失败追踪在 `test-results/`，不进版本控制。不要把真实用户图片或包含用户画面的追踪文件提交到仓库。

## 真实样本准备（未执行）

先准备 30 对清晰画前/画后样本：10 对调参、20 对验收，**参与者不能跨调参与验收两组**。另外准备至少 10 对未画、模糊或遮挡样本。样本应涵盖目标侧别、眼型与受控光线；保留实际失败案例，不能仅选成功图片。

清晰组需要包含三种方向标签 `close`、`high`、`low`。人工标注应先于算法结果，记录参考路径参数与真实眼尾方向；±10° 内标 `close`，超过 10° 分别为 `high`/`low`。使用同一眼部局部坐标，无法可靠标注的图片进不可判断组。边界样本另行备注，避免按模型结果反向修改标签。

在 `samples/private/` 放置图片与清单（该目录已忽略）。清单结构：

```json
{
  "samples": [
    {
      "id": "p01-01",
      "participant": "p01",
      "split": "tune",
      "label": "close",
      "before": "p01/before.jpg",
      "after": "p01/after.jpg",
      "side": "right",
      "angle": 20,
      "length": 0.3
    }
  ]
}
```

`split` 为 `tune`、`acceptance`、`unusable`；最后一组的 `label` 必须为 `unknown`。图片必须为原始摄像头画面，不能包含参考线或镜像后的截图。参与者使用匿名编号；图片文件相对清单路径解析。

执行：

```sh
npm run test:vision -- samples/private/manifest.json
```

本机脚本启动仅监听 `127.0.0.1` 的临时服务，通过真实 MediaPipe 提取每张图片的眼部位置，然后调用应用同一套 OpenCV 分析。每张静态照片使用独立检测器，避免不同人之间的时间跟踪状态串入。报告写到清单旁的 `report.json`，不包含原始图片。`durationMs` 是方向检查耗时，`pipelineMs` 包括图片、模型初始化等整条样本通路耗时，不代表实时跟踪帧率。

报告记录：

- 判断覆盖率 = 清晰验收样本中给出确定方向的数量 / 清晰验收样本总数。
- 拒判率 = 1 − 判断覆盖率。
- 条件正确率 = 正确的确定判断数 / 确定判断总数（无确定判断记为 0）。
- 不可判断组错误输出确定方向的数量，以及逐对耗时/原因。

通过门槛：调参至少 10 对、验收至少 20 对；清晰组覆盖率 ≥80%（20 对中至少 16 对），给出判断的结果正确率 ≥90%；不可判断组至少 10 对，全部不输出确定方向。新增更多样本时按相同比例计算。门槛是本次受控 Demo 的实验标准。

如果失败：记录失败类别，回到**调参组**调整规则，固定新版本，再使用新的独立验收集。不能反复在同一验收集调参后继续称其为独立验收。

## 实体手机检查（未执行）

先记录机型、系统、浏览器版本、访问方式 HTTPS、摄像头分辨率、光线与支架情况。在同一台主力手机连续完成五轮：

| 轮次 | 正常完整流程 | 轻微头动路径稳定 | 离开再进入不留旧线 | 暂停/重拍/换侧别 | 卡顿、崩溃、恢复耗时 |
|---|---|---|---|---|---|
| 1 | 待测 | 待测 | 待测 | 待测 | 待记录 |
| 2 | 待测 | 待测 | 待测 | 待测 | 待记录 |
| 3 | 待测 | 待测 | 待测 | 待测 | 待记录 |
| 4 | 待测 | 待测 | 待测 | 待测 | 待记录 |
| 5 | 待测 | 待测 | 待测 | 待测 | 待记录 |

补测：摄像头拒绝后授权恢复、屏幕旋转、切到后台再回来、锁屏/解锁、模型加载慢或中断、停止后摄像头指示关闭。不同品牌手机和 iOS Safari 不应从桌面 Chrome 测试结果推断兼容。

## 三位新手试用（未执行）

每人完成一侧操作，记录：是否理解左右眼、能否同时看提示和操作、是否需要支架、是否知道基准图需在画前采集、何时点击检查、是否理解“无法判断”、是否把“接近参考”误认为整体妆效合格、需要多少次外部协助。只记录实际观察，不把主观喜欢转成准确率。

最大待验证问题是：新增深色区域能否可靠地区分眼线、睫毛、阴影、皮肤褶皱和手部遮挡。若真实样本门槛未通过，交付名称保持“实时参考路径与分步练习 Demo”，不能验收为“分段关键帧纠偏已验证”。
